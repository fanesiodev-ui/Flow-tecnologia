# Copie este arquivo para .env e preencha os valores
# cp .env.example .env

# Porta do servidor (padrão: 8000)
PORT=8000

# Ambiente (development | production)
ENV=development

# (Futuro) Chave de API para integrações
# API_KEY=sua_chave_aqui
